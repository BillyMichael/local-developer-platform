apiVersion: v2
name: gitea-actions
description: Gitea Actions runner (act_runner with docker-in-docker)
version: 1.0.0
type: application

dependencies:
  - name: actions
    version: 0.1.2
    repository: https://dl.gitea.com/charts
