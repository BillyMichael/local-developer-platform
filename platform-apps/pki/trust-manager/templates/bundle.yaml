apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: ca
spec:
  sources:
  - secret:
      name: "root-ca"
      key: "tls.crt"
  target:
    configMap:
      key: "trust-bundle.pem"