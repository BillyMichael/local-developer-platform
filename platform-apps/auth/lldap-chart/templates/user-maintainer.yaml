---
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: lldap-maintainer-password
  annotations:
        argocd.argoproj.io/sync-wave: '-1'
spec:
  length: 32
  noUpper: false
  symbols: 5
  symbolCharacters: "_~."
  allowRepeat: true
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: lldap-maintainer-credentials
  annotations:
        argocd.argoproj.io/sync-wave: '-1'
spec:
  refreshInterval: 24h
  refreshPolicy: CreatedOnce
  target:
    name: lldap-maintainer-credentials
    creationPolicy: Owner
    template:
      type: Opaque
      data:
        id: "platform_maintainer"
        password: '{{`{{ .password }}`}}'
  dataFrom:
    - sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: lldap-maintainer-password